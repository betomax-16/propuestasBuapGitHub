<nav class="navbar navbar-inverse">
  <div class="container-fluid">
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <a class="navbar-brand" href="#">BUAP</a>
    </div>
    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
      <ul class="nav navbar-nav">
        {% if not is_granted('IS_AUTHENTICATED_FULLY') %}
        <li>
          <a href="{{ path('rcm_visitante_info') }}">Info</a>
        </li>
        <li>
          <a href="{{ path('rcm_visitante_contacto') }}">Contacto</a>
        </li>
        <li>
          <a href="{{ path('rcm_usuario_login') }}">Autenticarse</a>
        </li>
        {% endif %}
        {% if is_granted('ROLE_ADMIN') %}
        <li class="dropdown">
          <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">Usuarios<span class="caret"></span></a>
          <ul class="dropdown-menu">
            <li><a href="#">Lista de Usuarios</a></li>
            <li><a href="#">Registrar Usuario</a></li>
          </ul>
        </li>
        <li class="dropdown">
          <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">Propuestas<span class="caret"></span></a>
          <ul class="dropdown-menu">
            <li><a href="#">Lista de Propuestas</a></li>
            <li><a href="#">Registrar Propuesta</a></li>
          </ul>
        </li>
        {% endif %}
        {% if is_granted('ROLE_USER') %}
        <li>
          <a href="#">Mis Propuesta</a>
        </li>
        {% endif %}
      </ul>
      <ul class="nav navbar-nav navbar-right">
      {% if is_granted('IS_AUTHENTICATED_FULLY')%}
        <li>
          <a href="{{ path('rcm_usuario_logout') }}">Logout</a>
        </li>
      {% endif %}
      </ul>
    </div>
  </div>
</nav>
